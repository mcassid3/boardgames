<div class="all-games">

    <% @games.each do |game| %>
    <img class='thumbnail' src="<%= game.photo %>">
    <%= link_to game.title, game_path(game.id) %><br>
    <span class="more">
    <%= game.description %>
    </span>
    <% end %>
</div>

