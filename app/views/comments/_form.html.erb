
<%= form_for([@game, @game.comments.build]) do |f| %>

  <%= f.text_area :body, placeholder: 'Enter Comment Here', class: "materialize-textarea", id: 'comment-text-area' %><br>
  <%= f.submit 'Post Comment', class: "waves-effect waves-light btn-large " %>
<% end %>
